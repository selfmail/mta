# Api

This api is used to communicate with the Dashboard, Databases and SMTP Servers. 

**Main Features**
- Authentication -> `/users` 
- Health Checks -> `/health`
- Inbound SMTP Server communication (through the database)